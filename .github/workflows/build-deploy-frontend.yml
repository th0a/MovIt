name: build-frontend
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
jobs:
  build-frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: docker/setup-buildx-action@v2
      - name: Create docker image
        working-directory: ./frontend
        run: DOCKER_BUILDKIT=1 docker build --output type=tar,dest=frontend_webserver_docker_image.tar .
      - uses: actions/upload-artifact@v3
        with:
          name: frontend_webserver_docker_image.tar
          path: frontend/frontend_webserver_docker_image.tar

  deploy-frontend:
    if: ${{ startsWith(github.ref, 'refs/heads/main') }}
    needs: build-frontend
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    steps:
      - name: Download frontend webserver Docker image
        uses: actions/download-artifact@v1
        with:
          name: frontend_webserver_docker_image.tar

      - name: Configure AWS login
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: AKIAQL25RR5E7SNVEHFN #${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: 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 #${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: us-east-1

      - name: Login to AWS ECR
        id: ecr-login
        uses: aws-actions/amazon-ecr-login@v1

      - name: Upload Docker image to AWS ECR
        env:
          ECR_REGISTRY: ${{ steps.ecr-login.outputs.registry }}
          ECR_REPOSITORY:  movit #frontend_webserver
        run: |
          docker import frontend_webserver_docker_image.tar/frontend_webserver_docker_image.tar movit
          docker tag movit 025429118793.dkr.ecr.us-east-1.amazonaws.com/movit
          docker push $ECR_REGISTRY/movit
